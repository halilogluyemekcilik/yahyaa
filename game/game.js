document.addEventListener("DOMContentLoaded", () => {
    console.log("Game sayfası yüklendi 🎮");

    // İleride oyun listesini dinamik yüklemek için burası kullanılabilir.
    // Örn: JSON ile oyun bilgilerini alıp DOM'a eklemek.
});
